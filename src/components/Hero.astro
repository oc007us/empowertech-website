<section class="hero">
  <div class="hero-bg">
    <div class="hero-gradient-1"></div>
    <div class="hero-gradient-2"></div>
  </div>

  <div class="hero-content container">
    <h1 class="hero-headline">
      Governed Vertical AI,<br />
      <span class="gradient-text">Delivered Fast</span>
    </h1>

    <p class="hero-subheadline">
      Empower AI is an enterprise-grade platform that combines a semantic knowledge
      fabric, agentic orchestration, and deterministic model-driven generation to
      build accurate, auditable AI systems for regulated industries.
    </p>

    <div class="hero-proof-points">
      <div class="proof-point">
        <div class="proof-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            <path d="m9 12 2 2 4-4"/>
          </svg>
        </div>
        <div>
          <strong>No-hallucination by design</strong>
          <span>Evidence + provenance + constraints</span>
        </div>
      </div>

      <div class="proof-point">
        <div class="proof-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="3"/>
            <path d="M12 1v4M12 19v4M4.22 4.22l2.83 2.83M16.95 16.95l2.83 2.83M1 12h4M19 12h4M4.22 19.78l2.83-2.83M16.95 7.05l2.83-2.83"/>
          </svg>
        </div>
        <div>
          <strong>From knowledge to action</strong>
          <span>Agents that execute workflows across systems</span>
        </div>
      </div>

      <div class="proof-point">
        <div class="proof-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="2" y="2" width="20" height="8" rx="2"/>
            <rect x="2" y="14" width="20" height="8" rx="2"/>
            <line x1="6" y1="6" x2="6.01" y2="6"/>
            <line x1="6" y1="18" x2="6.01" y2="18"/>
          </svg>
        </div>
        <div>
          <strong>Enterprise-ready deployment</strong>
          <span>Private LLM hosting, VPC/on-prem, air-gapped</span>
        </div>
      </div>
    </div>

    <div class="hero-actions">
      <a href="/contact/" class="btn btn-primary btn-lg">Request a Demo</a>
      <a href="/platform/architecture/" class="btn btn-secondary btn-lg">See the Architecture</a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100dvh;
    display: flex;
    align-items: center;
    padding-top: var(--nav-height);
    overflow: hidden;
  }

  /* Animated background gradients */
  .hero-bg {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  .hero-gradient-1 {
    position: absolute;
    top: -30%;
    left: -20%;
    width: 80%;
    height: 80%;
    background: radial-gradient(circle, rgba(50, 69, 255, 0.12) 0%, transparent 60%);
    animation: float-1 12s ease-in-out infinite;
  }

  .hero-gradient-2 {
    position: absolute;
    bottom: -20%;
    right: -20%;
    width: 80%;
    height: 80%;
    background: radial-gradient(circle, rgba(188, 82, 238, 0.1) 0%, transparent 60%);
    animation: float-2 15s ease-in-out infinite;
  }

  @keyframes float-1 {
    0%, 100% { transform: translate(0, 0); }
    50% { transform: translate(30px, 20px); }
  }

  @keyframes float-2 {
    0%, 100% { transform: translate(0, 0); }
    50% { transform: translate(-20px, -30px); }
  }

  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    padding-block: var(--space-16);
  }

  .hero-headline {
    font-size: var(--font-size-hero);
    font-weight: var(--font-weight-bold);
    line-height: var(--line-height-tight);
    color: var(--color-white);
    margin-bottom: var(--space-8);
  }

  .hero-subheadline {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
    max-width: 720px;
    margin-inline: auto;
    margin-bottom: var(--space-12);
  }

  .hero-proof-points {
    display: flex;
    justify-content: center;
    gap: var(--space-8);
    margin-bottom: var(--space-12);
    flex-wrap: wrap;
  }

  .proof-point {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    text-align: left;
    max-width: 260px;
  }

  .proof-icon {
    flex-shrink: 0;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-md);
    background: rgba(50, 69, 255, 0.1);
    color: var(--color-accent-1);
  }

  .proof-point strong {
    display: block;
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
    color: var(--color-white);
    margin-bottom: var(--space-1);
  }

  .proof-point span {
    font-size: var(--font-size-xs);
    color: var(--color-text-muted);
  }

  .hero-actions {
    display: flex;
    justify-content: center;
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  .btn-lg {
    padding: var(--space-4) var(--space-10);
    font-size: var(--font-size-lg);
    border-radius: var(--radius-md);
  }

  @media (max-width: 768px) {
    .hero {
      min-height: auto;
      padding-top: var(--nav-height);
      padding-bottom: var(--space-8);
    }

    .hero-content {
      padding-block: var(--space-10) var(--space-8);
    }

    .hero-headline {
      font-size: clamp(2rem, 8vw, 3rem);
      margin-bottom: var(--space-6);
    }

    .hero-subheadline {
      font-size: var(--font-size-base);
      margin-bottom: var(--space-8);
    }

    .hero-proof-points {
      flex-direction: column;
      align-items: stretch;
      gap: var(--space-4);
      margin-bottom: var(--space-8);
      padding-inline: var(--space-2);
    }

    .proof-point {
      max-width: none;
    }

    .hero-actions {
      flex-direction: column;
      align-items: center;
    }

    .btn-lg {
      padding: var(--space-3) var(--space-8);
      font-size: var(--font-size-base);
      width: 100%;
      max-width: 320px;
    }

    .hero-gradient-1,
    .hero-gradient-2 {
      width: 120%;
      height: 60%;
      opacity: 0.7;
    }
  }
</style>
