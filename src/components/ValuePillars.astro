<section class="section pillars">
  <div class="container">
    <div class="pillars-header fade-in">
      <p class="section-label">Core Capabilities</p>
      <h2 class="section-title">Six Pillars of the Platform</h2>
      <p class="section-subtitle">
        Everything regulated enterprises need to build governed, operational AI systems &mdash; from knowledge ingestion to production deployment.
      </p>
    </div>

    <div class="pillars-grid">
      <div class="card pillar-card fade-in">
        <div class="pillar-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="5" r="3"/><circle cx="5" cy="19" r="3"/><circle cx="19" cy="19" r="3"/>
            <line x1="12" y1="8" x2="5" y2="16"/><line x1="12" y1="8" x2="19" y2="16"/>
            <line x1="5" y1="19" x2="19" y2="19"/>
          </svg>
        </div>
        <h3>Semantic Knowledge Fabric</h3>
        <p>Model-driven, semantically organized, hybrid retrieval (graph + vector + search), provenance-first. The foundation for accurate, explainable AI.</p>
      </div>

      <div class="card pillar-card fade-in">
        <div class="pillar-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="3"/>
            <path d="M12 1v4M12 19v4M4.22 4.22l2.83 2.83M16.95 16.95l2.83 2.83M1 12h4M19 12h4M4.22 19.78l2.83-2.83M16.95 7.05l2.83-2.83"/>
          </svg>
        </div>
        <h3>Agentic Orchestration Layer</h3>
        <p>Multi-agent planning + tool use + workflow execution. Build real systems of action that operate across teams and enterprise processes.</p>
      </div>

      <div class="card pillar-card fade-in">
        <div class="pillar-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            <path d="m9 12 2 2 4-4"/>
          </svg>
        </div>
        <h3>Governed &amp; Responsible AI</h3>
        <p>Guardrails, constraints, validation, evidence links, audit logs, and policy enforcement. Responsible AI is built in, not bolted on.</p>
      </div>

      <div class="card pillar-card fade-in">
        <div class="pillar-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
            <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
            <circle cx="12" cy="16" r="1"/>
          </svg>
        </div>
        <h3>Private &amp; Regulated Deployment</h3>
        <p>Private LLM hosting, VPC/on-prem options, air-gapped patterns, and data residency. Deploy anywhere your compliance demands.</p>
      </div>

      <div class="card pillar-card fade-in">
        <div class="pillar-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="2" y="3" width="20" height="14" rx="2"/>
            <path d="M8 21h8M12 17v4"/>
            <path d="M7 8h2M7 11h4"/>
            <circle cx="16" cy="10" r="2"/>
          </svg>
        </div>
        <h3>Multi-modal &amp; Multi-model</h3>
        <p>Text, tables, PDFs, images, voice/audio pipelines. Best-model routing ensures optimal performance across every modality.</p>
      </div>

      <div class="card pillar-card fade-in">
        <div class="pillar-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="16 18 22 12 16 6"/>
            <polyline points="8 6 2 12 8 18"/>
            <line x1="14" y1="4" x2="10" y2="20"/>
          </svg>
        </div>
        <h3>Deterministic Model-Driven Dev</h3>
        <p>Define models once, generate reliable architecture consistently. Saves tokens, improves consistency, and dramatically speeds delivery.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .pillars-header {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .pillars-header .section-subtitle {
    margin-inline: auto;
  }

  .pillars-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-6);
  }

  .pillar-card {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .pillar-icon {
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-md);
    background: linear-gradient(135deg, rgba(50, 69, 255, 0.1), rgba(188, 82, 238, 0.1));
    color: var(--color-accent-1);
  }

  .pillar-card h3 {
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-semibold);
    color: var(--color-white);
  }

  .pillar-card p {
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
    font-size: var(--font-size-sm);
  }

  @media (max-width: 1024px) {
    .pillars-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .pillars-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
