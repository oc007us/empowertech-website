---
import Layout from './Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
  date: string;
  author: string;
  readTime: string;
  tags: string[];
  heroImage?: string;
  ogImage?: string;
}

const { title, description, date, author, readTime, tags, heroImage, ogImage } = Astro.props;
---

<Layout title={`${title} — Empower AI Blog`} description={description} ogImage={ogImage}>
  <Navbar />
  <main>
    <article class="blog-post">
      <div class="blog-post-hero">
        <div class="blog-post-hero-bg">
          {heroImage && <img src={heroImage} alt="" class="blog-hero-image" />}
          <div class="blog-post-hero-overlay"></div>
          <div class="blog-post-gradient-1"></div>
          <div class="blog-post-gradient-2"></div>
        </div>
        <div class="blog-post-header container">
          <a href="/blog/" class="blog-back-link">&larr; All Posts</a>
          <p class="section-label">Blog</p>
          <h1 class="blog-post-title">{title}</h1>
          <div class="blog-post-meta">
            <span class="blog-meta-item">{date}</span>
            <span class="blog-meta-divider">&middot;</span>
            <span class="blog-meta-item">{author}</span>
            <span class="blog-meta-divider">&middot;</span>
            <span class="blog-meta-item">{readTime}</span>
          </div>
          <div class="blog-post-tags">
            {tags.map((tag) => (
              <a href={`/blog/?tag=${encodeURIComponent(tag)}`} class="blog-tag">{tag}</a>
            ))}
          </div>
        </div>
      </div>

      <div class="blog-post-body container">
        <div class="prose blog-prose">
          <slot />
        </div>
      </div>

      <div class="blog-cta container">
        <div class="blog-cta-card">
          <p class="blog-cta-heading">Ready to see governed AI in action?</p>
          <p class="blog-cta-desc">Learn how Empower AI helps regulated enterprises move from pilots to production-grade systems of action.</p>
          <div class="blog-cta-actions">
            <a href="/contact/" class="btn btn-primary">Request a Demo</a>
            <a href="/platform/" class="btn btn-secondary">Explore the Platform</a>
          </div>
        </div>
      </div>

      <div class="blog-post-footer container">
        <a href="/blog/" class="btn btn-secondary">&larr; Back to Blog</a>
      </div>
    </article>
  </main>
  <Footer />
</Layout>

<style>
  .blog-post-hero {
    position: relative;
    padding-top: var(--nav-height);
    overflow: hidden;
  }

  .blog-post-hero-bg {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  .blog-post-gradient-1 {
    position: absolute;
    top: -40%;
    left: -20%;
    width: 80%;
    height: 100%;
    background: radial-gradient(circle, rgba(50, 69, 255, 0.1) 0%, transparent 60%);
  }

  .blog-post-gradient-2 {
    position: absolute;
    bottom: -30%;
    right: -20%;
    width: 70%;
    height: 80%;
    background: radial-gradient(circle, rgba(188, 82, 238, 0.08) 0%, transparent 60%);
  }

  .blog-hero-image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.3;
  }

  .blog-post-hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, rgba(10, 11, 20, 0.4) 0%, rgba(10, 11, 20, 0.85) 100%);
  }

  .blog-post-header {
    position: relative;
    z-index: 1;
    padding-block: var(--space-12) var(--space-10);
    max-width: 800px;
  }

  .blog-back-link {
    display: inline-block;
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    margin-bottom: var(--space-6);
    transition: color var(--transition-fast);
  }

  .blog-back-link:hover {
    color: var(--color-accent-1);
  }

  .blog-post-title {
    font-size: var(--font-size-5xl);
    font-weight: var(--font-weight-bold);
    line-height: var(--line-height-tight);
    color: var(--color-white);
    margin-bottom: var(--space-6);
  }

  .blog-post-meta {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-4);
  }

  .blog-meta-item {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  .blog-meta-divider {
    color: var(--color-text-muted);
  }

  .blog-post-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .blog-tag {
    font-size: var(--font-size-xs);
    color: var(--color-accent-1);
    background: rgba(50, 69, 255, 0.1);
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-sm);
    border: 1px solid rgba(50, 69, 255, 0.2);
    text-decoration: none;
    transition: all var(--transition-fast);
  }

  .blog-tag:hover {
    background: rgba(50, 69, 255, 0.25);
    border-color: rgba(50, 69, 255, 0.4);
    color: var(--color-white);
  }

  .blog-post-body {
    padding-block: var(--space-10);
    max-width: 800px;
  }

  .blog-prose {
    border-top: 1px solid var(--color-border);
    padding-top: var(--space-10);
  }

  .blog-cta {
    max-width: 800px;
    padding-top: var(--space-10);
  }

  .blog-cta-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-10);
    text-align: center;
  }

  .blog-cta-heading {
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-white);
    margin-bottom: var(--space-3);
  }

  .blog-cta-desc {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
    margin-bottom: var(--space-6);
    max-width: 520px;
    margin-inline: auto;
  }

  .blog-cta-actions {
    display: flex;
    justify-content: center;
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  .blog-post-footer {
    max-width: 800px;
    padding-bottom: var(--space-16);
    border-top: 1px solid var(--color-border);
    padding-top: var(--space-8);
  }

  @media (max-width: 768px) {
    .blog-post-header {
      padding-block: var(--space-8) var(--space-6);
    }

    .blog-post-title {
      font-size: clamp(1.75rem, 6vw, 2.5rem);
    }

    .blog-post-meta {
      flex-wrap: wrap;
    }
  }
</style>
